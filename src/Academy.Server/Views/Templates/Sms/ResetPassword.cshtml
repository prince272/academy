@model (User User, ResetPasswordModel Form)
@{
    Layout = null;
}

Hello @(Model.User.FullName), The security code to reset your password is @(Model.Form.Code). If you didn’t request to reset your password, there’s nothing to worry about — you can safely ignore it.
