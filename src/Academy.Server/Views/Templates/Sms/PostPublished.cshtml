@model Post
@inject IOptions<AppSettings> AppSettingsAccessor
@{
    var appSettings = AppSettingsAccessor.Value;
    Layout = null;
}

Hello @(Model.Teacher.FullName), Your post with the title of "@(Model.Title)" has been reviewed and now published to our Academy. Check it out with this link: @($"{appSettings.Company.WebLink}/posts/{Model.Id}")