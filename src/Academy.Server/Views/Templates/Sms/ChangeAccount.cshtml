@model (User User, ChangeAccountModel Form)
@{
    Layout = null;
}

Hello @(Model.User.FullName), The security code to change your account is @(Model.Form.Code). If you didn’t request to change your account, there’s nothing to worry about — you can safely ignore it.